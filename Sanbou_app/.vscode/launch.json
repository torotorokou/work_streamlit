{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: manage_work",
            "type": "python",
            "request": "launch",
            "program": "${workspaceFolder}/app_pages/manage_work.py",
            "cwd": "${workspaceFolder}",
            "console": "integratedTerminal"
        }
    ]
}
