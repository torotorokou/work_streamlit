# app/config/main_paths.yaml

# 📄 CSVファイル関連パス
csv:
    receive_header_definition: 'data/master/receive_header_definition.csv' # 種別判定用ヘッダー
    required_columns_definition: 'config/required_columns_definition.csv' # 必須カラムの定義
    expected_dtypes: 'config/expected_dtypes.json' # データ型の期待定義
    unit_price_table: 'data/master/costs/unit_price_table.csv'
    transport_costs: 'data/master/costs/transport_costs.csv' #

# # excelテンプレートパス
# excel:
#     manage_template_path: 'data/master/abc_average_write_targets.csv'

# 📁 ディレクトリ構成
directories:
    input: 'data/input'
    output: 'data/output' # 出力結果の保存先
    master_data: 'data/master' # マスターCSVの格納場所
    template: 'data/template'
    temp: 'data/temp' # 一時ファイル格納
    models:
        models: 'data/models' # モデルの保存先
        predicted_import_volume: 'data/models/predicted_import_volume' # 予測搬入量の保存先

# 📜 ログ設定
logs:
    app: 'logs/app.log'
    error: 'logs/error.log'
    debug: 'logs/debug.log'

# yamlパス
config_files:
    app_config: 'config/app_config.yaml'
    expected_dtypes: 'config/expected_import_csv_dtypes.yaml'
    page_config: 'config/page_config.yaml'
    templates_config: 'config/templates_config.yaml'
    csv_sources_config: 'config/csv_sources_config.yaml'
    required_columns_definition: 'config/required_columns_definition.yaml'
    factory_manage_menu_config: '/work/app/config/factory_manage/menu_config.yaml'

# SQLデータベースパス
sql_database:
    weight_data: '/work/app/data/factory_manage/weight_data.db'
